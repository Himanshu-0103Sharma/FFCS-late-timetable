#include<iostream>
using namespace std;




int main(){
    int NumCourses,NumSlots;
    cout<<"Enter the Number of courses:";
    cin>>NumCourses;
    string courses[NumCourses];

    for(int i = 0; i < NumCourses; i++){
        cout<<"Enter Name of Course:";
        cin>>courses[i];
    }
    cout<<"Enter the Number of slots available:";
    cin>>NumSlots;
    string slots[NumSlots];

    for(int j = 0; j < NumSlots; i++){
        cout<<"Enter Name of Course:";
        cin>>Slots[j];
    }

    return 0;
}

string timetable(string courses[],string slots[]){
    while(sizeof(courses) != 0){
            continue;
    }
}





